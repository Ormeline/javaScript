//Define a function add (in a file add.js) which
// takes two arguments and return their sum.

const add = (a, b) => {
  return a + b;
};

module.exports = add;

//in node run console.log(add(4, 6));
